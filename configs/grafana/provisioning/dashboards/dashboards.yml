# Grafana Dashboard Provisioning Configuration

apiVersion: 1

providers:
  - name: 'Colima Services'
    orgId: 1
    folder: 'Colima Services'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards
